import { JalaliMomentPipe } from './jalali-moment.pipe';

describe('JalaliMomentPipe', () => {
  it('create an instance', () => {
    const pipe = new JalaliMomentPipe();
    expect(pipe).toBeTruthy();
  });
});

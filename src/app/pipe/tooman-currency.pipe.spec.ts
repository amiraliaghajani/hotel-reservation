import { ToomanCurrencyPipe } from './tooman-currency.pipe';

describe('ToomanCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ToomanCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
